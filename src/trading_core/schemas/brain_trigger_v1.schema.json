{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "brain_trigger_v1.schema.json",
  "title": "Brain Trigger v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema", "event_id", "timestamp", "symbol", "trigger_source",
    "brain_tier", "anomaly", "baseline", "market_context", "scheduler",
    "budget", "paper_only"
  ],
  "properties": {
    "schema": {"const": "brain_trigger_v1"},
    "event_id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,159}$"},
    "timestamp": {"type": "string", "format": "date-time"},
    "symbol": {"type": "string", "minLength": 1},
    "trigger_source": {"const": "azure_anomaly_dispatch"},
    "brain_tier": {"enum": ["L1", "L2", "L3"]},
    "anomaly": {
      "type": "object",
      "additionalProperties": true,
      "required": ["rule_id", "severity", "reason"],
      "properties": {
        "rule_id": {"type": "string", "minLength": 1},
        "severity": {"enum": ["LOW", "MEDIUM", "HIGH", "CRITICAL"]},
        "reason": {"type": "string", "minLength": 1},
        "observed_at": {"type": ["string", "null"], "format": "date-time"}
      }
    },
    "baseline": {
      "type": "object",
      "additionalProperties": false,
      "required": ["thesis_id", "plan_id"],
      "properties": {
        "thesis_id": {"type": "string", "minLength": 1},
        "plan_id": {"type": ["string", "null"]}
      }
    },
    "market_context": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repository", "branch", "path", "version"],
      "properties": {
        "repository": {"const": "doomit/trading-runtime"},
        "branch": {"const": "gpt-runtime"},
        "path": {"const": "runtime/market-context/current.json"},
        "version": {"type": ["string", "integer"]}
      }
    },
    "scheduler": {
      "type": "object",
      "additionalProperties": true,
      "required": ["minutes_to_next_deep", "state"],
      "properties": {
        "minutes_to_next_deep": {"type": "number", "minimum": 0, "maximum": 15},
        "state": {"enum": ["IDLE", "RUNNING", "COMPLETE", "LATE", "UNKNOWN"]}
      }
    },
    "budget": {
      "type": "object",
      "additionalProperties": false,
      "required": ["events_this_hour", "soft_cap", "hard_cap"],
      "properties": {
        "events_this_hour": {"type": "integer", "minimum": 0},
        "soft_cap": {"type": "integer", "minimum": 1},
        "hard_cap": {"type": "integer", "minimum": 2}
      }
    },
    "account_capacity": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "mode", "config_version",
        "open_contracts_total", "max_open_micro_contracts", "remaining_open_micro_contracts",
        "entries_this_session", "max_entries_per_session", "remaining_entries_this_session",
        "daily_realized_pnl_usd", "max_daily_realized_loss_usd", "remaining_daily_loss_usd",
        "consecutive_failures", "max_consecutive_losses", "remaining_loss_streak",
        "target_risk_per_trade_usd", "max_risk_per_trade_usd"
      ],
      "properties": {
        "mode": {"const": "PAPER"},
        "config_version": {"type": "string", "minLength": 1},
        "open_contracts_total": {"type": "integer", "minimum": 0},
        "max_open_micro_contracts": {"type": "integer", "minimum": 1},
        "remaining_open_micro_contracts": {"type": "integer", "minimum": 0},
        "entries_this_session": {"type": "integer", "minimum": 0},
        "max_entries_per_session": {"type": "integer", "minimum": 1},
        "remaining_entries_this_session": {"type": "integer", "minimum": 0},
        "daily_realized_pnl_usd": {"type": "number"},
        "max_daily_realized_loss_usd": {"type": "number", "exclusiveMinimum": 0},
        "remaining_daily_loss_usd": {"type": "number", "minimum": 0},
        "consecutive_failures": {"type": "integer", "minimum": 0},
        "max_consecutive_losses": {"type": "integer", "minimum": 1},
        "remaining_loss_streak": {"type": "integer", "minimum": 0},
        "target_risk_per_trade_usd": {"type": "number", "exclusiveMinimum": 0},
        "max_risk_per_trade_usd": {"type": "number", "exclusiveMinimum": 0}
      }
    },
    "coalesced_state_version": {"type": ["string", "integer", "null"]},
    "paper_only": {"const": true}
  }
}
